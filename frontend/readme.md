# Setup

## Directions for mac users
Create the virtual environment by running the following:

- Create the virtual environment flight_pred: `python3 -m venv flight_pred`
- Activate the env: `source flight_pred/bin/activate`
- Install the requirements: `pip install -r requirements.txt` from the folder frontend

